const Header = () => {
  return (
    <header className="app-header">
      <img
        className="app-logo"
        src="notes.png"
        alt="note icon"
        data-testid="app-logo"
      />
    </header>
  );
};

export default Header;
