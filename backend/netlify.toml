[build]
  command = "npm install && npm run build:netlify"
  functions = "src"
[functions]
  node_bundler = "esbuild"
[[redirects]]
  force = true
  from = "/api/*"
  status = 200
  to = "/.netlify/functions/api/:splat"